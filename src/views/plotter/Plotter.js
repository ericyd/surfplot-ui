'use strict';

import React, { Component } from 'react';

export default class Plotter extends Component {
    render () {
        return (
            <div>
                This is the plotter component
            </div>
        );
    }
}
